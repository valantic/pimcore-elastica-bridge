name: PHP Checks

on: [ push, pull_request ]


jobs:

  phpstan:

    name: phpstan (PHP ${{ matrix.php }} with Pimcore ${{ matrix.pimcore }} (${{ matrix.stability }}) on ${{ matrix.operating-system }})
    runs-on: ${{ matrix.operating-system }}

    strategy:
      fail-fast: false
      matrix:
        operating-system: [ ubuntu-latest ]
        php: [ '8.3', '8.4' ]
        pimcore: [ '^12.0' ]
        stability: [ prefer-lowest, prefer-stable ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: curl
          coverage: xdebug

      - name: Install dependencies
        run: |
          composer require "pimcore/pimcore:${{ matrix.pimcore }}" --no-interaction --no-update
          composer update --${{ matrix.stability }} --prefer-dist --no-interaction

      - name: List installed dependencies
        run: composer show -D

      - name: Set PHPStan baseline for prefer-lowest
        if: matrix.stability == 'prefer-lowest'
        run: echo "PHPSTAN_BASELINE=--configuration=phpstan-lowest.neon" >> $GITHUB_ENV

      - name: Static analysis using phpstan
        run: composer run phpstan -- --error-format=github $PHPSTAN_BASELINE

  php-cs-fixer:

    name: php-cs-fixer
    runs-on: ${{ matrix.operating-system }}

    strategy:
      fail-fast: false
      matrix:
        operating-system: [ ubuntu-latest ]

    steps:
      - uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.3
          extensions: curl
          coverage: xdebug

      - name: Install composer dependencies
        uses: ramsey/composer-install@v4

      - name: Ensure code style using php-cs-fixer
        run: composer run php-cs-fixer-check
